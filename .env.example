# cp .env.example .env  and edit
APP_DOMAIN=postrun.zuacaldeira.com

# :latest only ever moves from the default branch. Pin a branch or sha tag here
# to try a build out on the server without merging:
#   IMAGE_TAG=claude-video-full-width-card-aueowt
IMAGE_TAG=latest
